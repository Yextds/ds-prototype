---
{
    "stream": {
        "$id": "technology_listing-page",
        "source": "knowledgeGraph",
        "destination": "pages",
      "filter": {
        "entityTypes": [
          "ce_expertise"
        ],
        "savedFilterIds": [
          "924055047"
        ]
      },
      "fields": [
        "id",
        "uid",
        "name",
        "slug",
        "c_richTextDescription",
        "c_description1",
        "photoGallery",
        "c_relatedMenuCategory.name",
         "c_relatedMenuCategory.slug",
        "c_relatedMenuCategory.photoGallery",
        "c_relatedMenuCategory.c_description1",
        "c_relatedMenuCategory.c_richTextDescription"
      ],
      "localization": {
          "locales": ["en"]
      }
    },
    "urlFormat": "{{slug}}"
}
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>{{name}}</title>
    <meta name="description" content=Category page for {{name}}.>
    {{>head}}
    <link href="https://fonts.googleapis.com/css?family=Work+Sans:200,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://unpkg.com/flowbite@1.4.1/dist/flowbite.min.css" />
    <script src="https://unpkg.com/flowbite@1.4.1/dist/flowbite.js"></script>
    <script src="./TW-ELEMENTS-PATH/dist/js/index.min.js"></script>
    <style type="text/css">
        .topnav{
            background: black;
        }
       .mrigankFooter{
        background:black;
  }
    </style>

</head>


    <body>
        
    {{>navbar}}

    <div class="container py-8 px-6 mx-auto">
      <div class="uppercase tracking-wide no-underline hover:no-underline font-bold text-gray-800 text-2xl mb-8">{{name}}</div>
      <p class="mt-8 mb-8">{{convertRTFToHTML richTextDescription}}</p>
      <p class="mt-8 mb-8">{{convertRTFToHTML shortDescription}}</p>
    </div>

<div class="bg-black Nav ml-3 mr-11 rounded">
      <ul class="md:flex items-center bg-black justify-between text-base text-white pt-4 md:pt-0 py-4 px-5 my-2 rounded">
                     <li> {{#each c_relatedMenuCategory}}
                        <a class="inline-block no-underline hover:text-yellow-500 py-1 px-2" href={{slug}}>{{name}}</a>
                        
                       {{/each}}
                      </li>
                    </ul>

          </div>        
{{!-- cards --}}

<div  class="grid grid-cols-4 ml-3 object-center"> 
  {{#each c_relatedMenuCategory}} 
  <a href="{{slug}}">
        <div class="mb-3">
   <img src="{{photoGallery.[0].image.url}}" alt="">
            {{!-- <a class="mb-2 text-2xl font-bold tracking-tight text-gray-00 dark:text-white" href={{slug}}>{{name}}</a> --}}
          
        </div> 
    </a>      
        {{/each}}   
    </div> 
    <br>
    </body> 
 

<div class="w-full site-footer">
    <div class="centered-container">
      {{>footer}}
    </div>
  </div>

</html>
